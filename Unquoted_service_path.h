#include <iostream>

int main()
{
    // Get services that are NOT enclosed in Quotes
    // Service Path has SPACEs or not
    // User has WRITE ACCESS or not
    // User has right to RESTART service or not OR auto-start on reboot?
    return(0);
}
